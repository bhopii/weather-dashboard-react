import "./Header.css";
const Header = (props) => {

    return(
        <div className="tc fs-normal fl w-100 header-div">
            <h1>Weather Dashboard</h1>
        </div>
    );
}

export default Header;